import classNames from 'classnames/bind';

import styles from './Customer.module.scss';

const cx = classNames.bind(styles);

function Login() {
    return <h1>Customer management</h1>;
}

export default Login;
